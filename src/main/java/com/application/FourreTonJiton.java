package com.application;

public class FourreTonJiton {

	// TODO JT > Context Customizer lambda
	// factory.addContextCustomizers((context) -> {
	// final StandardRoot standardRoot = new StandardRoot(context);
	// final long cacheSize = 10000 * 1024;
	// standardRoot.setCacheMaxSize(cacheSize);
	// });

	// TODO JT : TEST > AttachListener
	// this.getView().getElement().addAttachListener(new ElementAttachListener() {
	//
	// @Override
	// public void onAttach(ElementAttachEvent event) {
	// final List<Pornstar> data = PornstarImageGridPresenter.this.getService().findAll();
	// PornstarImageGridPresenter.this.getView().setData(data);
	// }
	// });
	// }

	// TODO JT : TEST > Multiple RolesAllowed
	// @RolesAllowed({ "user", "admin" })

}
